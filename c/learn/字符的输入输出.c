#include<stdio.h>
#include<stdlib.h>
#include<string.h> 
void main(){
	char a[10];
	int i=-1;
	char ch; 
/*	scanf ("%s",a);
	printf ("%s",a);//空格、回车、Tab键会隔断 */
	
	/*while((ch=getschr())!='\0'){
		a[i]=getchar();
		i++;
		a[i]=getchar();
	}
	i=0;
	while(a[i]!=EOF){
		putchar(a[i]);
		i++;
		putchar(a[i]);
	}*/
	
	/*gets(a);
	puts(a);*/
}
